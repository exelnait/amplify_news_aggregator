version: 1
backend:
  phases:
    build:
frontend:
  phases:
    preBuild:
      commands:
        - npm install --silent
    build:
      commands:
        - nx build web-app
  artifacts:
    baseDirectory: packages/web-app/.next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*