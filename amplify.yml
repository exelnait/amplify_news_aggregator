version: 1
applications:
  - backend:
      phases:
        build:
          commands:
    frontend:
      phases:
        preBuild:
          commands:
            - npm install --silent
        build:
          commands:
            - cd packages/web-app
            - npm run build
            - ls
            - ls packages/web-app
      artifacts:
        baseDirectory: packages/web-app/build
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
      buildPath: /
