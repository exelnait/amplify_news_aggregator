version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - nx build web-app
  artifacts:
    baseDirectory: packages/web-app/.next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*